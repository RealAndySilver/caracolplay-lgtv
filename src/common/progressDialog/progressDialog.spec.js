describe('progressDialog section', function () {
    beforeEach(module('caracolplaylgtvapp.progressDialog'));

    it('should have a dummy test', inject(function() {
        expect(true).toBeTruthy();
    }));
});