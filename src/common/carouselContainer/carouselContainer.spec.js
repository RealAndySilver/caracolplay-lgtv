describe('carouselContainer section', function () {
		beforeEach(module('caracolplaylgtvapp.carouselContainer'));

		it('should have a dummy test', inject(function() {
				expect(true).toBeTruthy();
		}));
});